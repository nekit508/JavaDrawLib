apply plugin: "java"

ext {
    jarName = "JavaDrawLib"
    jarPath = "C:\\Users\\Никита\\Desktop"

    vers = "v1.0"

    javaUtilsVersion = "v3.3"
}

repositories {
    mavenCentral()
    maven { url "https://jitpack.io" }
}

wrapper {
    gradleVersion = '7.4.2'
}

jar {
    archiveFileName = "${jarPath}\\${jarName}-${vers}.jar"
}

dependencies {
    compileOnly "com.github.nekit508:JavaUtils:${javaUtilsVersion}"
}